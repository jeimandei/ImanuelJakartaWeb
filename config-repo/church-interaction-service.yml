server:
  port: 8086

app:
  public-url: https://gmimimanueljakarta.or.id

spring:
  data:
    jpa:
      repositories:
        bootstrap-mode: deferred
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:your-email@gmail.com}
    password: ${MAIL_PASSWORD:your-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        debug: false
